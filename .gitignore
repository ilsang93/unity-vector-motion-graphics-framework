# Unity-generated metadata for project state, not for the package
.DS_Store
Thumbs.db

# IDE / editor
.vs/
.idea/
*.user
*.suo
*.userprefs

# Build artifacts (should never live inside a UPM package, but guard anyway)
[Bb]in/
[Oo]bj/
*.csproj
*.sln

# Logs
*.log

# Internal / dev-only notes — not shipped with the package
BACKLOG.md
BACKLOG.md.meta
TODO.md
TODO.md.meta
NOTES.md
NOTES.md.meta
SCRATCH.md
SCRATCH.md.meta

# Author-time-only files not shipped with the package would go here.
# (AnimatorSample/ was retired when the Animator JSON track was dropped
#  in 2026-06-13, then re-introduced in 0.37.0 with DSL-based .vmgfx
#  scripts — the directory is shipped again.)
